name: Test and Deploy Python app to Az Function App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        working-directory: my-function-app
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        working-directory: my-function-app
        run: |
          source venv/bin/activate
          pytest

      - name: Package app for deployment
        run: |
          cd my-function-app
          zip -r ../release.zip . -x "venv/*" ".git/*" ".github/*" "__pycache__/*"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-function-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: python-function-app

      - name: Unzip artifact
        run: unzip release.zip

      - name: Deploy to Azure Function App
        uses: azure/functions-action@v1
        with:
          app-name: 'your-function-app-name'  # Replace with your Function App name
          publish-profile: ${{ secrets.AZURE_APP_PUBLISH_PROFILE }}  # Replace with your secret publish profile
          package: './release.zip'
