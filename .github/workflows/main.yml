name: Deploy Flask App to Azure Function

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # Install dependencies from requirements.txt
      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      # Zip the app (excluding unnecessary files like venv, .git, etc.)
      - name: Zip the app
        run: |
          zip -r function.zip . -x "venv/*" ".git/*" ".github/*" "__pycache__/*"

      # Upload the zip artifact for the deploy job
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: flask-function-app
          path: function.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: flask-function-app

      # Set up Azure CLI to deploy the app
      - name: Set up Azure CLI
        uses: azure/setup-azurecli@v2

      # Deploy the app to Azure Function App using the publish profile
      - name: Deploy to Azure Function App
        run: |
          az functionapp deployment source config-zip \
            --resource-group RG1 \
            --name flaskapp1 \
            --src function.zip \
            --publish-profile ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
